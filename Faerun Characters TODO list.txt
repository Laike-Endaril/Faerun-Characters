Fix horizontal sliders' mouse tracking; it's got the wrong offsets for the ends

Make sure all sliders actually save and load data correctly



Systems for all standard render modes, for NPCs as well as players



Blood config (on/off), in file only, not in config GUI
